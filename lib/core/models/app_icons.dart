import 'package:flutter/material.dart';

class AppIcons {
  final IconData? icon;
  final String? semanticLabel;
  final String? id;

  AppIcons({required this.icon, required this.semanticLabel, this.id});
}